import numpy as np

bar = np.zeros((2))
foo = np.zeros((3, 2))
pos = np.zeros((2, 3, 2))

print(bar.shape)
print(f"bar.shape: {bar.shape}")
print(f"foo.shape: {foo.shape}")
print(f"pos.shape: {pos.shape}")


print(bar)
print(foo)
print(pos)